# Phase 0 — schema validation only.
# Ingest/extract dependencies (httpx, GitPython, an LLM client) get added at Phase 1/3.
pyyaml>=6.0
jsonschema>=4.20

# LLM extraction (Phase 3) — OpenAI-compatible client, covers LM Studio and OpenRouter alike.
openai>=1.60
